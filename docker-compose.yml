services:
  app:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
      - "3001:3001"
    environment:
      - CHOKIDAR_USEPOLLING=1
    command: sh -c "npm install && npm run dev"
